'use client';

import { useEffect, useState } from 'react';
import { Save } from 'lucide-react';

type Settings = { enabled: boolean; accountNumber: string; accountType: string; instructions: string; minimumAmount: number; maximumAmount: number | null };
const defaults: Settings = { enabled: false, accountNumber: '', accountType: 'merchant', instructions: '', minimumAmount: 0, maximumAmount: null };

export default function PaymentSettingsPage() {
  const [settings, setSettings] = useState<Settings>(defaults); const [message, setMessage] = useState(''); const [error, setError] = useState(''); const [saving, setSaving] = useState(false);
  useEffect(() => { fetch('/api/admin/payment-settings').then((response) => response.json()).then((data) => { if (data.success) setSettings({ ...defaults, ...data.settings }); else setError(data.error || 'Unable to load settings.'); }).catch(() => setError('Unable to load settings.')); }, []);
  const save = async (event: React.FormEvent) => { event.preventDefault(); setSaving(true); setMessage(''); setError(''); try { const response = await fetch('/api/admin/payment-settings', { method: 'PUT', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(settings) }); const data = await response.json(); if (!response.ok) throw new Error(data.error || 'Unable to save settings.'); setSettings({ ...defaults, ...data.settings }); setMessage('Payment settings saved.'); } catch (reason) { setError(reason instanceof Error ? reason.message : 'Unable to save settings.'); } finally { setSaving(false); } };
  return <div className="mx-auto max-w-3xl space-y-7"><section><p className="text-xs font-bold uppercase tracking-[0.24em] text-primary">Payment configuration</p><h2 className="mt-2 text-3xl font-black">bKash Payment Settings</h2><p className="mt-2 text-sm text-white/55">Configure the public account and manual verification instructions.</p></section>{error ? <div className="rounded-xl border border-red-400/30 bg-red-500/10 p-3 text-sm text-red-200">{error}</div> : null}{message ? <div className="rounded-xl border border-primary/30 bg-primary/10 p-3 text-sm text-primary">{message}</div> : null}<form onSubmit={save} className="space-y-5 rounded-2xl border border-white/10 bg-white/[0.04] p-6 shadow-2xl"><label className="flex items-center gap-3 text-sm font-bold"><input type="checkbox" checked={settings.enabled} onChange={(event) => setSettings({ ...settings, enabled: event.target.checked })} className="accent-primary" /> Enabled</label><label className="block text-xs font-bold uppercase tracking-wider text-white/55">bKash Number<input required value={settings.accountNumber} onChange={(event) => setSettings({ ...settings, accountNumber: event.target.value })} placeholder="017XXXXXXXX" className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm text-white outline-none focus:border-primary" /></label><label className="block text-xs font-bold uppercase tracking-wider text-white/55">Account Type<select value={settings.accountType} onChange={(event) => setSettings({ ...settings, accountType: event.target.value })} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm text-white outline-none focus:border-primary"><option value="merchant">Merchant</option><option value="personal">Personal</option></select></label><label className="block text-xs font-bold uppercase tracking-wider text-white/55">Payment Instructions<textarea value={settings.instructions} onChange={(event) => setSettings({ ...settings, instructions: event.target.value })} rows={7} placeholder="Send the exact plan amount, then submit your sender number and transaction ID." className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm text-white outline-none focus:border-primary" /></label><div className="grid gap-4 sm:grid-cols-2"><label className="block text-xs font-bold uppercase tracking-wider text-white/55">Minimum Amount<input type="number" min="0" value={settings.minimumAmount} onChange={(event) => setSettings({ ...settings, minimumAmount: Number(event.target.value) })} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm text-white" /></label><label className="block text-xs font-bold uppercase tracking-wider text-white/55">Maximum Amount<input type="number" min="0" value={settings.maximumAmount ?? ''} onChange={(event) => setSettings({ ...settings, maximumAmount: event.target.value ? Number(event.target.value) : null })} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm text-white" /></label></div><button disabled={saving} className="inline-flex items-center gap-2 rounded-xl bg-primary px-5 py-3 text-sm font-bold text-[#06251b]"><Save className="h-4 w-4" />{saving ? 'Saving...' : 'Save Changes'}</button></form></div>;
}
